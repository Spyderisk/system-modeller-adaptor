# coding: utf-8

"""
    OpenAPI definition

    SPYDERISK System Modeller (SSM) REST API definitions for domain models, user system models and usage by other applications.

    The version of the OpenAPI document: v3.5.0
    Contact: info@spyderisk.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from ssmclientlib.models.control_set import ControlSet
from ssmclientlib.models.level import Level
from typing import Optional, Set
from typing_extensions import Self

class ControlStrategy(BaseModel):
    """
    ControlStrategy
    """ # noqa: E501
    uri: Optional[StrictStr] = None
    label: Optional[StrictStr] = None
    description: Optional[StrictStr] = None
    parents: Optional[List[StrictStr]] = None
    blocking_effect: Optional[Level] = Field(default=None, alias="blockingEffect")
    mandatory_control_sets: Optional[Dict[str, ControlSet]] = Field(default=None, alias="mandatoryControlSets")
    optional_control_sets: Optional[Dict[str, ControlSet]] = Field(default=None, alias="optionalControlSets")
    threat_csg_types: Optional[Dict[str, StrictStr]] = Field(default=None, alias="threatCsgTypes")
    enabled: Optional[StrictBool] = None
    id: Optional[StrictStr] = None
    __properties: ClassVar[List[str]] = ["uri", "label", "description", "parents", "blockingEffect", "mandatoryControlSets", "optionalControlSets", "threatCsgTypes", "enabled", "id"]

    @field_validator('threat_csg_types')
    def threat_csg_types_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        for i in value.values():
            if i not in set(['BLOCK', 'MITIGATE', 'TRIGGER']):
                raise ValueError("dict values must be one of enum values ('BLOCK', 'MITIGATE', 'TRIGGER')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ControlStrategy from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of blocking_effect
        if self.blocking_effect:
            _dict['blockingEffect'] = self.blocking_effect.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each value in mandatory_control_sets (dict)
        _field_dict = {}
        if self.mandatory_control_sets:
            for _key_mandatory_control_sets in self.mandatory_control_sets:
                if self.mandatory_control_sets[_key_mandatory_control_sets]:
                    _field_dict[_key_mandatory_control_sets] = self.mandatory_control_sets[_key_mandatory_control_sets].to_dict()
            _dict['mandatoryControlSets'] = _field_dict
        # override the default output from pydantic by calling `to_dict()` of each value in optional_control_sets (dict)
        _field_dict = {}
        if self.optional_control_sets:
            for _key_optional_control_sets in self.optional_control_sets:
                if self.optional_control_sets[_key_optional_control_sets]:
                    _field_dict[_key_optional_control_sets] = self.optional_control_sets[_key_optional_control_sets].to_dict()
            _dict['optionalControlSets'] = _field_dict
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ControlStrategy from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "uri": obj.get("uri"),
            "label": obj.get("label"),
            "description": obj.get("description"),
            "parents": obj.get("parents"),
            "blockingEffect": Level.from_dict(obj["blockingEffect"]) if obj.get("blockingEffect") is not None else None,
            "mandatoryControlSets": dict(
                (_k, ControlSet.from_dict(_v))
                for _k, _v in obj["mandatoryControlSets"].items()
            )
            if obj.get("mandatoryControlSets") is not None
            else None,
            "optionalControlSets": dict(
                (_k, ControlSet.from_dict(_v))
                for _k, _v in obj["optionalControlSets"].items()
            )
            if obj.get("optionalControlSets") is not None
            else None,
            "threatCsgTypes": obj.get("threatCsgTypes"),
            "enabled": obj.get("enabled"),
            "id": obj.get("id")
        })
        return _obj


